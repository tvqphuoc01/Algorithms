#include <bits/stdc++.h>
#include <string>
using namespace std;
int main()
{
    string input;
    cin >> input;
    
    int count = 1;
    for (int i = 0; i < input.length(); i++)
    {
        if (input[i] < 97)
        {
            count++;
        }
    }
    cout << count;
}
