#include <bits/stdc++.h>

using namespace std;

int main(){
    int n,k,q;
    cin >> n >> k >> q;
    int arr[n];
    for(int i = 0; i < n; i++){
        cin >> arr[(i+k)%n];
    }
    int ans;
    for(int i = 0; i < q;i++){
        cin >> ans;
        cout << arr[ans] << endl;
    }
}
